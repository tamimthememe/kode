import React from "react";

const Logo = ({className}) => {
  return <h1 className={`text-5xl max-sm:text-4xl text-green-600 tracking-tighter font-medium cursor-default ${className}`}>Kode!</h1>;
};

export default Logo;
